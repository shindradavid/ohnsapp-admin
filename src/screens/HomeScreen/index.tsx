import { View, Text } from 'react-native';
import { MainPageLayout } from '../../components/layout';

const HomeScreen = () => {
  return (
    <MainPageLayout title={`OHN'S TRANSPORTATION`}>
      <Text>Home screen</Text>
    </MainPageLayout>
  );
};

export default HomeScreen;
